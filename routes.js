// GLOBAL
const HOME = "/";
const ADD = "/add";

// API
const API = "/api";
const GET_DATA = "/data";
const GET_STRDATE = "/strdate";

const routes = {
  home: HOME,
  add: ADD,
  api: API,
  getData: GET_DATA,
  getStrDate: GET_STRDATE,
};

export default routes;
